var app = require('../app');
var config = require('../config');

app.listen(config.port, function(err ,res){
    if(err){
    console.log(err);
    };
    console.log('server running at port ' + config.port + ' in ' + config.mode + ' mode');
});